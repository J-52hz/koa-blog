const params_newCategory = {
  type: 'object',
  required: ['ll_category_val', 'll_category_name']
}

const params_id = {
  type: 'object',
  required: ['ll_id']
}

export { params_newCategory, params_id }
